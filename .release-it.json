{
  "git": {
    "requireCleanWorkingDir": false,
    "commitMessage": "chore(release): bump mobile version to ${version}"
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "after:bump": "npx capacitor-set-version -v ${version} -b $(node -p \"const v='${version}'.split('.'); Number(v[0])*10000 + Number(v[1])*100 + Number(v[2])\")"
  }
}